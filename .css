:root {
    --primary: #FF6B6B;
    --secondary: #4ECDC4;
    --accent: #FFE66D;
    --bg: #F7FFF7;
    --text: #2F2F2F;
}

* { box-sizing: border-box; }

body {
    font-family: 'Comic Sans MS', cursive, sans-serif;
    background-color: var(--bg);
    color: var(--text);
    margin: 0;
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
}

.container {
    width: 95%;
    max-width: 800px;
    background: white;
    padding: 20px;
    border-radius: 30px;
    box-shadow: 0 10px 0px var(--secondary);
    text-align: center;
    border: 4px solid #333;
}

.page { display: none; animation: pop 0.4s ease-out; }
.active { display: block; }

@keyframes pop {
    0% { transform: scale(0.9); opacity: 0; }
    100% { transform: scale(1); opacity: 1; }
}

h1, h2 { color: var(--primary); text-shadow: 2px 2px var(--accent); }

/* Buttons */
button {
    padding: 12px 20px;
    font-size: 1.1rem;
    margin: 8px;
    border: 3px solid #333;
    border-radius: 15px;
    cursor: pointer;
    transition: 0.2s;
    font-weight: bold;
}

button:active { transform: translateY(2px); }
.btn-main { background-color: var(--accent); width: 80%; max-width: 300px; }
.btn-nav { background-color: var(--secondary); color: white; }
.btn-nav.small { font-size: 0.9rem; padding: 8px 15px; }
.btn-exit { background-color: var(--primary); color: white; }

input {
    padding: 15px;
    font-size: 1.1rem;
    border-radius: 15px;
    border: 3px solid var(--secondary);
    width: 80%;
    margin-bottom: 20px;
}

/* Grid Layout */
.grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(130px, 1fr));
    gap: 10px;
    margin: 20px 0;
}

.card {
    background: #fff;
    border: 3px solid #eee;
    padding: 15px;
    border-radius: 20px;
    cursor: pointer;
    transition: 0.3s;
}

.card:hover { border-color: var(--secondary); background: #f0ffff; }
.accent-card { background: #fffdf0; border-color: var(--accent); }

/* Canvas Styling */
.canvas-container {
    background: white;
    border: 3px dashed #ccc;
    margin: 15px 0;
    border-radius: 15px;
    overflow: hidden;
    touch-action: none; /* Penting untuk mobile */
}

#coloringArea {
    width: 100%;
    height: 300px;
    display: block;
}

.palette { display: flex; justify-content: center; gap: 10px; flex-wrap: wrap; }
.color-dot { 
    width: 40px; height: 40px; 
    border-radius: 50%; 
    border: 3px solid #333; 
    cursor: pointer; 
}